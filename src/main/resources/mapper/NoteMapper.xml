<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maihehe.blogcore._04_mapper.NoteMapper">
    <insert id="batchInsert">
        INSERT INTO maihehe_note (id, topic_path, current_path, name, parent_path, is_folder, visible, updated_time)
        VALUES
        <foreach collection="notes" item="note" separator=",">
            (#{note.id}, #{note.topicPath}, #{note.currentPath}, #{note.name}, #{note.parentPath},
            #{note.isFolder}, #{note.visible}, #{note.updatedTime})
        </foreach>
    </insert>

    <delete id="deleteByPaths">
        DELETE FROM maihehe_note
        WHERE current_path IN
        <foreach collection="notes" item="n" open="(" separator="," close=")">
            #{n.currentPath}
        </foreach>
    </delete>
</mapper>
